PACKAGES:
  - bash-completion
  - git
  - wget
  - tmux
  - vim
  - curl
  - gnupg
  - apt-transport-https
  - ca-certificates
  - containerd.io
REPO_GPGKEY:
  - https://pkgs.k8s.io/core:/stable:/v1.31/deb/Release.key
SELINUX_POLICY: default
services:
  - containerd
  - kubelet
kubeadm_config_file: kubeadm_config_containerd.yaml
